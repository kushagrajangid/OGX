<%- include('./partials/head') %>
<body class="min-h-screen bg-gray-50 flex items-center justify-center px-4 py-12">

  <div class="w-full max-w-md">

    <!-- Header -->
    <div class="text-center mb-8">
      <h1 class="text-2xl font-semibold text-gray-900">Welcome back</h1>
      <p class="text-sm text-gray-500 mt-1">Sign in to your account</p>
    </div>

    <!-- Card -->
    <div class="bg-white border border-gray-200 rounded-2xl p-8">
      <form action="/postlogin" method="POST" class="space-y-5">

        <!-- Email -->
        <div>
          <label for="email" class="block text-sm font-medium text-gray-700 mb-1.5">
            Email address
          </label>
          <input
            type="email"
            id="email"
            name="email"
            placeholder="you@example.com"
            required
            class="w-full px-4 py-2.5 text-sm border border-gray-300 rounded-lg
                   focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent
                   placeholder-gray-400 transition"
          />
        </div>

        <!-- Password -->
        <div>
          <label for="password" class="block text-sm font-medium text-gray-700 mb-1.5">
            Password
          </label>
          <input
            type="password"
            id="password"
            name="password"
            placeholder="••••••••"
            required
            class="w-full px-4 py-2.5 text-sm border border-gray-300 rounded-lg
                   focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent
                   placeholder-gray-400 transition"
          />
        </div>

        <!-- Error message -->
        <% if (typeof error !== 'undefined' && error) { %>
          <p class="text-sm text-red-500 bg-red-50 border border-red-200 rounded-lg px-4 py-2.5">
            <%= error %>
          </p>
        <% } %>

        <!-- Login Button -->
        <button type="submit"
          class="w-full bg-blue-600 hover:bg-blue-700 active:scale-[0.98] text-white
                 text-sm font-medium py-2.5 rounded-lg transition-all duration-150">
          Sign in
        </button>

        <!-- Divider -->
        <div class="flex items-center gap-3">
          <div class="flex-1 h-px bg-gray-200"></div>
          <span class="text-xs text-gray-400">or</span>
          <div class="flex-1 h-px bg-gray-200"></div>
        </div>

        <!-- Signup Button -->
        <a href="/register"
          class="block w-full text-center border border-gray-300 hover:bg-gray-50
                 active:scale-[0.98] text-gray-700 text-sm font-medium py-2.5 rounded-lg
                 transition-all duration-150">
          Create an account
        </a>
            <% if(error){ %>
  <p class="text-sm text-red-500 mt-2"><%= error %></p>
              <% } %>
      </form>
    </div>

  </div>

</body>
</html>